{
  "name": "fish-h5-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/gen-manifest.js && vite",
    "build": "node scripts/gen-manifest.js && tsc --noEmit && vite build && node scripts/copy-assets.js",
    "preview": "vite preview",
    "gen-manifest": "node scripts/gen-manifest.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "^3.85.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
